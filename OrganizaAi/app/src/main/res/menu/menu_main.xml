<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <!-- Opção para limpar tarefas concluídas -->
    <item
        android:id="@+id/action_delete_completed"
        android:title="@string/menu_delete_completed"
        android:icon="@drawable/ic_delete_sweep"
        app:showAsAction="ifRoom"
        android:contentDescription="Limpar tarefas concluídas" />
    
    <!-- Opção de configurações -->
    <item
        android:id="@+id/action_settings"
        android:title="@string/menu_settings"
        android:icon="@drawable/ic_settings"
        app:showAsAction="never"
        android:contentDescription="Configurações do aplicativo" />
    
</menu>
